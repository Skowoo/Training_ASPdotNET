@model IEnumerable<Rectangle>

    <h1>Rectanglesy</h1>

    <ul>
    @foreach (var item in Model)
    {
        <li>
            <a asp-action="Details" asp-route-id="@item.Id">@item.Area m<sup>2</sup>, @item.CreatedAt</a>
        </li>
    }
    </ul>